# Project Overview

LocalUtopia — Unity 게임 프로젝트. 현재 Qix(영역 확보) 게임을 개발 중.

-   **타겟 해상도**: 1080 x 1920 (세로)

------------------------------------------------------------------------

# 문서

폴더 구조, 씬 흐름, 핵심 로직, 스테이지 데이터는 `README.md`가 단일 출처다.
작업 전에 README를 보고, 구조나 로직을 바꿨으면 README도 같이 고친다.

------------------------------------------------------------------------

# 코드 컨벤션

-   **필드 노출**: `[SerializeField] private` 대신 **`public` 필드**를 사용한다.
-   **필드명**: 언더바(`_`) 접두사 사용 **금지**. `camelCase` 그대로 사용.
-   **네임스페이스**: `Qix` 폴더의 게임 로직만 `namespace Qix`. 씬 스크립트·유틸·플레이어는 전역 네임스페이스
    (`Player` 폴더에 `namespace Player`를 두면 `Player.Player`가 되어 어색하다).
-   **주석**: 한국어, WHY가 비자명할 때만 작성

------------------------------------------------------------------------

# 메모리 관리

Unity도 C# GC가 있지만, WPF와 릭이 나는 지점이 다르다. 일반 C# 객체(리스트, POCO
등)는 GC에 맡겨도 되지만, 아래 항목은 **명시적으로 해제**해야 한다.

-   **런타임에 생성한 `UnityEngine.Object`** (`Texture2D`, `Material`, `Mesh`,
    `RenderTexture`, `AudioClip` 등): C# 래퍼가 GC 되어도 네이티브 메모리는
    회수되지 않는다. `new`로 만들었으면 `OnDestroy()`에서 `Destroy()` 호출.
-   **`Instantiate()`로 생성한 GameObject**: 씬 그래프에 남아 있으면 GC 대상이
    아니다. 필요 없어지면 `Destroy()`로 명시 제거.
-   **이벤트 구독(`+=`)**: 특히 static 이벤트나 싱글톤 서비스 구독은
    `OnDisable()`/`OnDestroy()`에서 반드시 `-=`.
-   **코루틴**: 소유 오브젝트가 사라지기 전에 `StopCoroutine()` /
    `StopAllCoroutines()`.

------------------------------------------------------------------------

# 작업 방식

-   **Simplicity First**: 최소 변경, 근본 원인 해결. 필요한 곳만 건드림.
-   **기존 작업 보호**: 변경 대상 파일 먼저 확인. 요청 범위 밖 리팩터링 금지.
-   **Memory Optimize**: 작업은 모두 메모리 최적화에도 중점을 맞춰야함.